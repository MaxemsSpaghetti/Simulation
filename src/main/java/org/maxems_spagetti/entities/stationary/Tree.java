package org.maxems_spagetti.entities.stationary;

import org.maxems_spagetti.map.Coordinates;
import org.maxems_spagetti.entities.Entity;

public class Tree extends Entity {

    public Tree(Coordinates coordinates) {
        super(coordinates);
    }
}
